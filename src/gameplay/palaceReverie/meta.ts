import { PALACE_LAGOON } from "../../world/heightmap";

/** Lightweight first-approach/minimap metadata for Palace Reverie. */
export const REVERIE_CENTER = {
  x: PALACE_LAGOON.x - 18,
  z: PALACE_LAGOON.z + 8
} as const;
