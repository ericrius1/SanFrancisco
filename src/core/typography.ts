/**
 * Canvas 2D cannot resolve CSS custom properties, so runtime-painted labels
 * share the same self-hosted family as the DOM interface through this stack.
 */
export const CANVAS_FONT_FAMILY =
  '"Newsreader", "Iowan Old Style", "Palatino Linotype", Palatino, Georgia, serif';
